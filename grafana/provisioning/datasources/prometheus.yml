apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090/prometheus # имя сервиса из docker-compose
    isDefault: true
    editable: false # запрещаем менять через UI — только через код
